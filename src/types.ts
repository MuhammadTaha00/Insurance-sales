export type Sale = {
  id?: string;
  firstName: string;
  lastName: string;
  email: string;
  age: number;
  product: string;
};

export type Product = {
  id: string;
  value: string;
  label: string;
};
